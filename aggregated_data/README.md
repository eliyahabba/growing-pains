Expected inputs (not included in the repository):

- `tinybenchmarks/` — `lb.pickle`, `mmlu_fields.pickle`, `helm_lite.pickle`, etc., per `config/data_source_config.json`
- `aggregated/` — `helm_lite_aggregated.parquet`, `helm_classic_aggregated.parquet` when using aggregated sources

Copy from your AdaptEval data store or regenerate from upstream TinyBenchmarks / HELM exports.
